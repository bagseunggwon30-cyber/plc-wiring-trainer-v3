<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="PlcWiringTrainer.App.Controls.WiringCanvas"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    AutomationProperties.AutomationId="WiringCanvas"
    AutomationProperties.Name="결선 캔버스"
    IsTabStop="True"
    Unloaded="WiringCanvas_Unloaded"
    mc:Ignorable="d">
    <Grid Background="#111827">
        <canvas:CanvasControl
            x:Name="NativeCanvas"
            ClearColor="#111827"
            CreateResources="Canvas_CreateResources"
            Draw="Canvas_Draw"
            PointerMoved="Canvas_PointerMoved"
            PointerPressed="Canvas_PointerPressed"
            PointerReleased="Canvas_PointerReleased"
            PointerWheelChanged="Canvas_PointerWheelChanged" />

        <Border
            x:Name="WireHint"
            Margin="14"
            Padding="10,6"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Background="#E61E293B"
            CornerRadius="6"
            Visibility="Collapsed">
            <TextBlock x:Name="WireHintText" Foreground="White" />
        </Border>

        <Border
            Margin="14"
            Padding="9,5"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Background="#D91E293B"
            CornerRadius="6">
            <TextBlock x:Name="ZoomText" Foreground="#CBD5E1" Text="100%" />
        </Border>
    </Grid>
</UserControl>
