# PLC Wiring Trainer v2.5.0 업데이트

v2.5의 핵심은 하나의 실제 배선 데이터(`S.wires`)를 세 가지 방식으로 편집하는 것입니다.

- **🧰 실물**: 기존 제어반 장비 이미지와 실제 단자 결선
- **📄 결선도**: 종이 도면처럼 장비 블록과 단자 번호를 보고 결선
- **🔁 시퀀스**: NO/NC/코일/램프 심볼 중심의 제어 회로
- **I/O**: PLC 단자, XG5000 P 주소, 연결 상대 표

## 사용

1. 기존처럼 실물 화면에서 장비를 배치합니다.
2. 상단 `📄 결선도` 또는 `🔁 시퀀스`를 누릅니다.
3. 단자 원을 한 번 클릭하고 다른 단자 원을 클릭하면 와이어가 생성됩니다.
4. 이 와이어는 별도의 도면용 복사본이 아니라 **실제 `S.wires`에 저장**되므로 실물 화면에도 즉시 나타납니다.
5. 와이어를 클릭하고 `Delete` 또는 `⌫ 선택선 삭제`를 누르면 모든 보기에서 함께 삭제됩니다.
6. 심볼 제목을 드래그하면 해당 도면에서의 배치만 이동하며 실제 제어반 장비 위치는 변하지 않습니다.

`examples/v25-self-hold-three-view.json`을 가져오면 자기유지 회로 예제를 바로 확인할 수 있습니다.

## 저장 스키마

프로젝트 스키마가 7로 변경되었습니다. 기존 장비/와이어 데이터 형식은 유지하고 다음 필드를 추가했습니다.

```json
{
  "workspaceView": "panel",
  "diagramLayouts": {
    "schematic": {},
    "sequence": {}
  }
}
```

따라서 전기적 연결은 한 번만 저장되고, 종이 결선도/시퀀스 심볼의 화면 좌표만 별도로 저장됩니다.

## 시퀀스 심볼

현재 전용 심볼을 제공하는 주요 장비:

- PB-1C / PB-NO / PB-NC
- LIMIT
- SEL-2P / SEL-3P
- MC: 코일, 13-14 NO, 21-22 NC, 3극 주접점
- MY2N: 코일 + 2개의 전환접점
- TIMER / FLICKER: 코일 + 2개의 전환접점
- EOCR: 전원, 트립 NC/NO, 알람 접점
- LAMP / BUZZER / SOL-Y

그 외 PLC·HMI·단자대·센서·모듈은 실제 단자 ID를 유지한 I/O 블록으로 표시되며, 단자가 많으면 여러 블록으로 자동 분할됩니다.

## 주의

v2.5는 종이 결선도와 시퀀스 회로의 **양방향 단자 결선 편집 기반**을 추가한 버전입니다. IEC/JIS 완전 도면 CAD, 자동 rung 재배치, 접점 cross-reference, PDF 도면 양식 등은 후속 범위입니다.
